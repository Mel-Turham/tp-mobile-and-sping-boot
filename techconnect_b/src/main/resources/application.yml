server:
  port: 8080

spring:
  application:
    name: techconnect-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/techconnect
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:melturham06}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: tchoikuemel06@gmail.com
    password: kwiy ovqq cify tidk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true

app:
  jwt:
    secret: ${JWT_SECRET:mySecretKey123456789012345678901234567890}
    expirationMs: 86400000
  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:8081}

logging:
  level:
    com.techconnect: DEBUG
    org.springframework.security: DEBUG
